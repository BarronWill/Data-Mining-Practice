import numpy as np
import pandas as pd


class HierarchyCluster:
    def __init__(self):
        self.cluster = None

    def find_distance(self, point1, point2):
        return np.
    def __find_closet(self, mat):
        len(mat-1)
    def fit(self, X):
        distance_mat = np.sqrt(((X[:, np.newaxis] - X) ** 2).sum(axis=2))
